package ${packageName}.dao.mapper;

import ${packageName}.dao.entity.${ClassName};
import com.sika.common.dao.mapper.BaseMapper;
import org.apache.ibatis.annotations.Param;

/**
 * ${className}Mapper接口
 *
 * @author ${author}
 * @date ${datetime}
 */
public interface ${ClassName}Mapper extends BaseMapper<${ClassName},${pkColumn.javaType}>
{
    #foreach($column in $indexColumns)
    ${ClassName} selectBy${column.columnName}( #foreach($indexColumn in ${column.genTableColumns})@Param("${indexColumn.columnName}")$indexColumn.javaType $indexColumn.javaField#if( $foreach.hasNext ),#end#end);

    #end
}
